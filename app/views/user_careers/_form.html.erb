<%= form_for(@user_career) do |f| %>
  <% if @user_career.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_career.errors.count, "error") %> prohibited this user_career from being saved:</h2>

      <ul>
      <% @user_career.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :career_id %><br />
    <select name="user_career[career_id]">
    <% Career.all.each do | c | %>
     <option value='<%= c.id %>'><%= c.name %></option>
    <% end %>
  </select>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
